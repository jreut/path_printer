require 'spec_helper'

describe PathPrinter do
  it 'has a version number' do
    expect(PathPrinter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
